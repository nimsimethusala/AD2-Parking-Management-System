server:
  port: 8082

spring:
  application:
    name: parking-service
  datasource:
    url: jdbc:mysql://localhost:3306/smart_parking_management_system_parkingdb
    username: my_username
    password: my_password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
